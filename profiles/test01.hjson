{
  server: 'trex.straylight.goosga.ng',                // set to your trex astf server host/ip
  dutName: 'loopback',                                // for display only 
  runName: 'someRunIdentifier',                               // for display only
  sleep: 0.5,                                         // duration to sleep between perf samples
  duration: 60,                                       // test duration - not a hard limit
  testProfiles: [                                     // array of profiles to run
    {
      name: 'http_simple2',                           // for display only
      mult: 1400,                                     // multiplier/amplification factor
      file: 'trex-core/scripts/astf/http_simple2.py', // astf file to run
    }
  ]
}
